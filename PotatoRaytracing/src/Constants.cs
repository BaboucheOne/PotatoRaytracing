using System;

namespace PotatoRaytracing
{
    public static class Constants
    {
        public const double INV_PI = 1 / Math.PI;
        public const double INV_DOUBLE_PI = 1 / (Math.PI * 2);
    }
}
